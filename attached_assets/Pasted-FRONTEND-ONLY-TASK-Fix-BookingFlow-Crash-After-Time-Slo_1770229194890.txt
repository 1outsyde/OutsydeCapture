FRONTEND-ONLY TASK: Fix BookingFlow Crash After Time Slot Selection (Do NOT break availability or banner)

Do NOT change any backend code. Do NOT change base availability saving. Do NOT change banner or availability tab logic. Only fix BookingFlow time-slot selection and confirm rendering.

Problem

The booking calendar now loads correctly and slots are generated by backend, but after selecting a time slot the app crashes with:

Cannot read property 'name' of undefined

This happens ONLY after slot selection, meaning the Confirm/Summary step renders an object that does not exist or assumes a .name field on a slot object.

Required Fix (Do these in order)
1) Find the Exact .name Access Causing the Crash

Search in BookingFlow and its confirm/summary components for:

.name access after slot selection
Examples:

selectedSlot.name

booking.timeSlot.name

selectedTime.name

booking.selectedSlot.name

Add a temporary log to confirm what the object actually is when a slot is selected.

2) Fix the Data Contract: Slots Have start/end, Not name

A time slot must be treated like:

type Slot = { start: string; end: string; available?: boolean };


Update the Confirm/Summary UI to display time like:

formatTime(slot.start) + " - " + formatTime(slot.end)

NOT slot.name.

3) Ensure Booking State Is Complete Before Rendering Confirm Step

Before rendering the Confirm step, add a guard:

if (!selectedService || !selectedDate || !selectedSlot?.start || !selectedSlot?.end) {
  return null; // or show a small loading/placeholder
}


This prevents render crashes during state transitions.

4) Fix Slot Selection Handler So It Always Sets a Valid Slot Object

When a user taps a slot, ensure the state updates to:

setSelectedSlot({ start: slot.start, end: slot.end });


Not a partial object, not undefined, and not a different shape.

5) Add Safe Rendering Everywhere Slot Is Used

Replace unsafe accesses with optional chaining:

selectedSlot?.start
selectedSlot?.end

Completion Criteria

Selecting a date shows time slots ✅

Selecting a time slot does NOT crash ✅

Confirm step shows service name and formatted time range ✅

Continue to payment works ✅

Base hours, banner, and availability tab remain unchanged ✅